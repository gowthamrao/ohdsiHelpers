% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CreateDataTree.R
\name{createDataTree}
\alias{createDataTree}
\title{Create a Hierarchical Data Tree from a Data Frame}
\usage{
createDataTree(
  df,
  childField,
  parentField,
  numberOfLevels = 999,
  rootName = "RootNode"
)
}
\arguments{
\item{df}{A data frame containing the hierarchical data.}

\item{childField}{(character) The name of the column in \code{df} representing the child identifiers (e.g., "id").}

\item{parentField}{(character) The name of the column in \code{df} representing the parent identifiers (e.g., "parentId").}
}
\value{
A \code{Node} object from the \code{data.tree} package representing the hierarchical structure. The node names are taken from the "name" column (if present) or the child ID column.
}
\description{
This function converts a flat data frame representing a hierarchical structure (with parent-child relationships) into a \code{Node} object from the \code{data.tree} package.
}
\details{
This function assumes that the input data frame \code{df} contains at least three columns:
\itemize{
\item \code{childField}: A unique identifier for each child node.
\item \code{parentField}: The identifier of the parent node for each child.
\item \code{name}: A descriptive name for each node (optional). If not provided, the child ID will be used as the node name.
}

The function validates the input, checks for the presence of a root node, and constructs the hierarchical tree using the \code{data.tree::as.Node} function.
}
